# anezkabereckova.com

Web módní designérky Anežky Bereckové. Statický Next.js export, obsah jako data v repu.

```bash
npm ci
npm run generate
npm run dev
npm run build
```

Fotky ze zdrojové složky `sources/` (není v gitu) se převádějí příkazem `npm run assets`.
